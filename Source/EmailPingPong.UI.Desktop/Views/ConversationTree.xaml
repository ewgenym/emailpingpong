<UserControl x:Class="EmailPingPong.UI.Desktop.Views.ConversationTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EmailPingPong.UI.Desktop.ViewModels"
			 xmlns:utils="clr-namespace:EmailPingPong.UI.Desktop.Utils"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock Text="Group by:" Grid.Row="0" Grid.Column="0" />
		<ComboBox Grid.Row="1" Grid.Column="0" 
				  ItemsSource="{Binding Source={utils:Enumeration {x:Type local:GroupBy}}}"
				  DisplayMemberPath="Description"
				  SelectedValue="{Binding Path=GroupBy}"
				  SelectedValuePath="Value"/>
		<TextBlock Text="Search in:" Grid.Row="3" Grid.Column="0" />
		<ComboBox Grid.Row="4" Grid.Column="0" 
				  ItemsSource="{Binding Source={utils:Enumeration {x:Type local:SearchIn}}}"
				  DisplayMemberPath="Description"
				  SelectedValue="{Binding Path=SearchIn}"
				  SelectedValuePath="Value"/>
		<TreeView ItemsSource="{Binding Items}"
		          Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" 
				  PreviewMouseLeftButtonDown="TreeView_PreviewMouseLeftButtonDown"
				  Name="conversationTree">
			<TreeView.ItemContainerStyle>
				<Style TargetType="{x:Type TreeViewItem}">
					<Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
					<Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
					<Setter Property="FontWeight" Value="{Binding IsUnread, Mode=OneWay}" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding IsUnread}" Value="True">
							<Setter Property="FontWeight" Value="Bold" />
						</DataTrigger>
						<DataTrigger Binding="{Binding IsUnread}" Value="False">
							<Setter Property="FontWeight" Value="Normal" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</TreeView.ItemContainerStyle>
			<TreeView.Resources>
				<HierarchicalDataTemplate DataType="{x:Type local:CommentViewModel}" ItemsSource="{Binding Answers}">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>
						
						<TextBlock Grid.Column="0" Grid.Row="0" Text="{Binding Author}" FontStyle="Italic" Foreground="Gray" Margin="0,0,5,0" />

						
						<TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding CreatedOn, StringFormat={}{0:MMMM dd\, yyyy} at {0:t}}" Foreground="Gray" />

						<TextBlock Grid.ColumnSpan="2" Grid.Row="1" Text="{Binding Body}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="38" MinHeight="38" />
					</Grid>
				</HierarchicalDataTemplate>
				<HierarchicalDataTemplate DataType="{x:Type local:ConversationViewModel}" ItemsSource="{Binding Comments}">
					<StackPanel>
						<TextBlock Text="{Binding Topic}" />
					</StackPanel>
				</HierarchicalDataTemplate>
				<HierarchicalDataTemplate DataType="{x:Type local:FolderViewModel}" ItemsSource="{Binding Comments}">
					<Grid Margin="8">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto"/>
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>
						<Border BorderThickness="0.6" BorderBrush="DarkGray">
						</Border>
						<TextBlock Text="{Binding Folder}" />
					</Grid>
				</HierarchicalDataTemplate>
			</TreeView.Resources>
		</TreeView>
	</Grid>
</UserControl>